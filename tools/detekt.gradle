detekt {
    version = rootProject.ext.detekt
    profile("main") {
        input = "$projectDir"
        config = "$projectDir/default-detekt-config.yml"
        filters = ".*test.*,.*/resources/.*,.*/tmp/.*"
        output = "$projectDir/build/reports"
        outputName = "yosef"
    }
}
